apiVersion: v1
kind: ConfigMap
metadata:
  name: bank-backend-config
  namespace: bank-app
data:
  DB_HOST: "REPLACE_WITH_RDS_ENDPOINT"
  DB_PORT: "3306"
  DB_NAME: "pod10fintech_db"
  CORS_ALLOWED_ORIGIN: "https://bank.cohort5pod10.online"
